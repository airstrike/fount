[package]
name = "picker"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
fount = { path = "../..", features = ["office"] }
iced = { version = "0.15.0-dev", features = ["tokio"] }
tokio = { version = "1", features = ["rt"] }
